#include <stdio.h> //biblioteca padrao de entrada e saida de dados.

int main () {
//função principal do programa int= inteiro, main= nome da função, ()= parametros da função, {}= corpo da função.
  
  printf("Hello World!\n"); //função que imprime na tela.
  return 0; //return 0; //retorna 0 para o sistema operacional, significa que o programa foi executado com sucesso.
}

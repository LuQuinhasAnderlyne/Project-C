#include <stdio.h>

int main()
{
  /*
  uma variavel tem uma estrutura de declaração, e precisamos seguir esta base a seguir para declarar uma variavel: 
  
  tipo nomeDaVariavel = atribuir um valor padrao;

  Ou seja:   int num = 0; int = tipo da variavel, num = nome da variavel, 0 = valor padrao atribuido a variavel.
  */

  // int num, num2 = 0; é forma de declarar mais de uma variavel, deixando o codigo elegante e limpo.


  // Exemplo de nome composto:
  double numeroVerificado;

  /* 
  quando uma variavel tem um nome composto por mais de uma palavra (como no exemplo acima), usamos o camelCase, ou seja, a primeira palavra com letra minuscula e as outras com letra maiuscula.

   no C uma variavel pode comecar com qualquer letra, menos com numeros, mas se utilizarmos o caracter _ (underline), ai podemos iniciar com numeoros ou ate separar um nome composto.

   Exemplo: int _2numeros;  int num_eros;
   
   Obs: nao pode usar nenhum caracter especial no nome, apenas o _ (underline). 
  */

   return 0;
}

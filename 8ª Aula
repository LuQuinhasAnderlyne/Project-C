#include <stdio.h>

int main() {

    /*  scanf: funcao de entrada, para fazer leitura de dados, ela lê do teclado tudo oque o usuario digitol.
          ela e semelhante a funcao "printf", entao temos que especificar o tipo de dados estamos esperando e o usuario digite, e qual é a variavel que vai receber estes dados:

          scanf("tipo_de_entrada", variavel_que_recebera);

          Tipos de entradra: %i, %f, %c e etc.
          
          O parametro "variavel_que_recebera", nos devemos especificar onde vamos armazenar oque o usuario digitol, ou seja, passar a variavel aonde queremos armazenar este valor (a variavel deve ser precedida pelo caracter &).

          Como fariamos para receber 2 valores ao mesmo tempo?

          scanf("%tipo1 %tipo2", &var1, &var2);

        */

    // Aplicação real:
   
    int num1, num2;
    printf("Por favor, digite um numero:\n");
    scanf("%i", &num1);

    printf("%i", num1);

    return 0;
}

#include <stdio.h>

int main() 
{
  /*
  Uma variavel possui uma estrutura de declaracao, e precisamos seguir esta estrutura para declarar ela:

  <tipo> <nome> = <valor>;
  ou seja : int num = 10;

  para declararmos mais de uma variavel podemos utilizar um atalho:

  int num, num2 = 10;
  alem de ser mais rapido, fica mais elegante e deixa o codigo mais facil de ler.
*/

  /*
  Quando uma variavel possui um nome composto, para diferenciarmos o primeiro nome do segundo, utilizamos o underline (_), por exemplo: 

  double numeroVerificador;
 
  Obs: podemos utilizar numeros no nome da variavel, mais nao podemos iniciar o nome da variavel com um numero, por exemplo: int 1num;

  Outra regra e que o nome da nossa variavel nao pode conter espaco, por exemplo: int num eros; pois o compilador vai achar que esquecemos de colocar ou uma virgula ou ponto e virgula, e ele bai gerar um erro. Para isso utilizamos o underline (_) nessa situcao, dividindo o nome da variavel em dois, por exemplo: int num_eros;
  

*/
  return 0;
}
